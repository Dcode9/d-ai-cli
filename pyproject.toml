[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "d-ai"
version = "0.1.0"
description = "D'Ai CLI — pure chat terminal interface with the same multi-provider engine as D-Ai web. Looks like the Python REPL. No login required."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [
    { name = "Dhairya Shah", email = "dcode9@users.noreply.github.com" }
]
keywords = ["ai", "cli", "chat", "llm", "groq", "gemini", "terminal"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "httpx>=0.27.0",
    "rich>=13.0.0",
]

[project.urls]
Homepage = "https://github.com/Dcode9/d-ai-cli"
Repository = "https://github.com/Dcode9/d-ai-cli"
Issues = "https://github.com/Dcode9/d-ai-cli/issues"

[project.scripts]
d-ai = "dai.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/dai"]

[tool.hatch.build.targets.sdist]
include = [
    "/src",
    "/README.md",
    "/LICENSE",
]
